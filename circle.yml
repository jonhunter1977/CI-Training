machine:
  timezone:
    Europe/London
  node:
    version: 6.10.2
  environment:
    PATH: "${PATH}:${HOME}/${HR-REACT}/node_modules/.bin"
dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn
test:
  override:
    - yarn test    
# general:
#   artifacts:
#     - "../artefacts/*"
# deployment:
#   master:
#     branch: master
#     commands:
#       - "grunt circle:prod"
#   develop:
#     branch: develop
#     commands:
#       - "grunt circle:qa"
# notify:
#   webhooks:
#     - url: http://webhooks.laterooms.com/webhooks/circleci
